spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code_db?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  

  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0
  elasticsearch:
    uris: http://localhost:9200
    username: 
    password: 

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.yww.code.entity
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    root: INFO
    com.yww.code: DEBUG
  file:
    name: logs/application.log
